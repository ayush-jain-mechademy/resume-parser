# Copy to .env and fill in. The tool reads GEMINI_API_KEY from the environment.
# Get a key at https://aistudio.google.com/apikey  — enable billing before using
# real candidate data so prompts are not used to train Google's models.
GEMINI_API_KEY=your-key-here
